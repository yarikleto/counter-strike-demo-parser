# TASK-077: Golden file: entity counts + classes

**Milestone:** 7 — Performance & Polish
**Status:** `TODO`
**Size:** S | **Type:** vertical-slice
**Depends on:** TASK-075

**Goal:** Create golden file for entity system statistics: total entities created, per-ServerClass counts, max concurrent entities.

**Acceptance Criteria:**
- [ ] Golden file contains: total unique entities, per-class creation counts, max concurrent entity count
- [ ] Test verifies current counts match golden file

**Cycle:** developer (implements + tests) -> reviewer
